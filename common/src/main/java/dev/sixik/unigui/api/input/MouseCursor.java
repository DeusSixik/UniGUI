package dev.sixik.unigui.api.input;

/** Standard mouse cursor shapes supported by UI backends. */
public enum MouseCursor {
    DEFAULT,
    POINTER,
    TEXT,
    CROSSHAIR,
    RESIZE_HORIZONTAL,
    RESIZE_VERTICAL
}
