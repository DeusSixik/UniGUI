package dev.sixik.unigui.api.animation;

/**
 * Built-in numeric widget properties that can be driven by the default transition engine.
 */
public enum AnimatedProperty {
    OPACITY,
    POSITION_X,
    POSITION_Y,
    SCALE_X,
    SCALE_Y,
    ROTATION_DEGREES,
    PIVOT_X,
    PIVOT_Y
}
