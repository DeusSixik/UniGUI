package dev.sixik.unigui.api.input;

/**
 * Помечает контейнер, собственные границы которого не должны становиться целью hit-test.
 *
 * <p>Hit tester продолжает обходить дочерние элементы контейнера, но не возвращает
 * попадание, если указатель находится вне всех детей. Это нужно полноэкранным overlay-host,
 * внутри которых размещена небольшая интерактивная панель.</p>
 */
public interface HitTestChildrenOnly {
}
