package dev.sixik.isf.definition;

/** Тип значения, доступного визуальному документу и будущему редактору. */
public enum IsfParameterType {
    NUMBER,
    BOOLEAN,
    STRING,
    RESOURCE_LOCATION,
    ITEM,
    COLOR,
    JSON
}
